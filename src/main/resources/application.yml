server.port: 8082

spring:
  application:
    name: billing
  datasource:
    url: 'jdbc:postgresql://localhost:5432/ordering'
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: 'true'
  flyway:
    enabled: false
